-- create sequence for dimension table
create or replace sequence dimdate_seq start = 1 increment = 1;

create or replace sequence dimplatform_seq start = 1 increment = 1;

create or replace sequence dimsite_seq start = 1 increment = 1;

create or replace sequence dimvideo_seq start = 1 increment = 1;