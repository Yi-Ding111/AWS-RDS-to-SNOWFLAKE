-- create raw data table
create or replace table video_raw ( "DATETIME" VARCHAR2(30), 
                                   "VIDEOTITLE" VARCHAR2(200), 
                                   "EVENTS" VARCHAR2(150)
                                   );